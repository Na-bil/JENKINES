---
- name: copie fichier webapp
  hosts: all
  become: true


  tasks:

    - name: copy file
      template:
        copy:
        src: /home/ansadmin/tomcat-ansible/webapp.war
        dest: /opt/tomcat/latest/webapps

